// Package viz implements the web-based UOp graph visualizer, compiled Go→WASM;
// it runs the real rewrite engine and scheduler in-browser and renders the actual
// UOp graph — never a mock. See DESIGN.md §7 for the visualizer thesis.
package viz
