name: CI

on:
  workflow_dispatch:
  pull_request:
    branches: ["main"]

jobs:
  go:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Install Vulkan SW renderer
        run: |
          sudo apt-get update
          sudo apt-get install -y mesa-vulkan-drivers vulkan-tools libvulkan1
        env:
          DEBIAN_FRONTEND: noninteractive

      - name: Build
        run: go build ./...

      - name: Test
        # -p 1 keeps peak memory bounded: the Vulkan SW renderer (lavapipe)
        # plus a second heavy package in parallel can OOM the hosted runner.
        #
        # GOMEMLIMIT caps the Go heap's transient growth so the runtime returns
        # garbage to the OS aggressively. The onnx e2e suite (ResNet-9 multi /
        # symbolic batch) peaks ~15 GB un-capped, which exceeds the 16 GB hosted
        # runner once lavapipe's host-side buffer overhead is added, and the
        # runner SIGTERMs the step (exit 143, no Go traceback). 8 GiB trims ~4 GB
        # of heap garbage with no measurable time cost (live heap stays well
        # under the limit, so there is no GC death-spiral).
        run: go test -short -timeout=12m -p 1 -coverprofile=coverage.txt ./...
        env:
          GOTRACEBACK: crash
          GOMEMLIMIT: 8GiB

      # NOTE to maintainer: add CODECOV_TOKEN as a repository secret in
      # repo Settings, Secrets and variables, Actions. Without it, uploads
      # from private repos or protected forks will fail.
      - name: Upload Go coverage to Codecov
        uses: codecov/codecov-action@v6
        with:
          files: coverage.txt
          flags: backend
          token: ${{ secrets.CODECOV_TOKEN }}

      - name: Vet
        run: go vet ./...

      - name: Lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: latest

  web:
    # Frontend (web/studio.js, web/worker.js) tested under jsdom with Vitest.
    # Separate job so the studio coverage upload is independent of the Go job.
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Set up Node
        uses: actions/setup-node@v6.4.0
        with:
          node-version: lts/*
          cache: npm

      - name: Install deps
        run: npm ci

      - name: Test with coverage
        run: npm run coverage

      - name: Upload web coverage to Codecov
        uses: codecov/codecov-action@v6
        with:
          files: coverage-js/lcov.info
          flags: frontend
          token: ${{ secrets.CODECOV_TOKEN }}
