# anneal studio accessibility checklist

Status: binding. This is the gate every web view (W0 foundation, W1+ feature
views) ships through. If your view does not pass every item below, it does not
ship. Cross-refs: `DESIGN.md §11`, `notes/anneal_web_spec.md §10`.

No em-dashes anywhere in this doc, per project convention.

---

## §0. What this doc is

`A11Y.md` is the operational form of `DESIGN.md §11` (the binding a11y
invariants). DESIGN says what; A11Y says how to verify it. When a new view
lands, the PR description quotes the checklist below and marks each item.

The hard constraint, from George: "the web stuff should be as accessible as
possible." Read: WCAG 2.2 Level AA across every surface, with AAA where it is
cheap (touch target size, link/button distinction, focus indicator
strength). The W0 foundation bakes this in so every subsequent view inherits
the discipline; do not break the foundation when adding W1+ content.

---

## §1. Per-view checklist

Copy this list into the PR description for each new view. Every box must be
checked before merge.

- [ ] **Heading hierarchy.** The active view exposes exactly one `<h1>`
  (others may be `visually-hidden` if the view chrome already names the
  surface). Inactive `.view` sections are `display:none`, so their `<h1>`
  elements are hidden from the a11y tree. No `<h3>` before `<h2>`.
- [ ] **Landmarks present.** Page has `<header role="banner">`,
  `<nav role="navigation" aria-label>`, `<main role="main" id="main">`,
  and `<footer role="contentinfo">`. New view content lives inside `<main>`.
- [ ] **Skip link** is the first focusable element and lands on `#main`.
- [ ] **Focus visible.** Every interactive element shows a focus ring via
  the global `:focus-visible` rule. Do not write `outline: none` without a
  replacement. The studio's focus token is `--focus-ring` (gold).
- [ ] **Focus order matches DOM order.** No `tabindex` greater than 0.
  `tabindex="-1"` is allowed for programmatically-focused targets like
  `<main>` itself.
- [ ] **Keyboard reachable.** Every interactive control is reachable by Tab
  and operable by Enter / Space (buttons) or normal text input (inputs).
  No mouse-only affordances.
- [ ] **No keyboard traps.** Focus can always escape every container. The
  keyboard-help modal traps Tab within it but Escape always exits.
- [ ] **ARIA labels.** Every icon-only button has an `aria-label` that
  describes the action ("close keyboard shortcuts", not "X"). Decorative
  SVGs have `aria-hidden="true"` and `focusable="false"`.
- [ ] **Color contrast.** Body text >= 4.5:1 on its background. UI
  component borders, focus indicators, and large text >= 3:1. The
  brand-token verification lives in `cmd/anneal/cmd_web_test.go`
  (`TestWebA11Y_BrandTokenContrast`); a new background or text token added
  to `studio.css` must be added to that test.
- [ ] **DD1: color is never alone.** Any place that uses teal / ember /
  gold for meaning pairs the color with shape, label, or weight. The
  active nav state is teal + left border + font weight bump.
- [ ] **Live region updates.** SPA route change announces "navigated to
  {view}" via `#live-region`. SSE-driven views (W5 train, W6 generate)
  announce new events the same way (`announce()` in `studio.js`).
- [ ] **Document title** updates on view change. Format:
  `anneal · {view}`.
- [ ] **`lang="en"` on `<html>`** is intact. If your view introduces non-
  English content (e.g. a code identifier in a non-Latin script), mark it
  with `lang` on the element.
- [ ] **Touch target 44x44.** Every clickable element meets the minimum
  hit area (WCAG 2.5.5 AAA). Verify with the dev-tools accessibility
  pane.
- [ ] **Text resize.** Layout works at 200% browser zoom with no
  horizontal scroll. Type sizes use `rem`, not `px`. Verify by zooming.
- [ ] **Reduced motion.** Every animation site checks
  `@media (prefers-reduced-motion: reduce)` and disables or shortens. The
  global `*` rule in `studio.css` is the backstop; per-animation rules
  are the belt.
- [ ] **Forced colors.** Critical UI (nav active state, focus ring, links)
  is verified in Windows High Contrast mode. CSS uses system color
  keywords (CanvasText, Highlight, LinkText) in the `forced-colors:
  active` block.
- [ ] **Prefers contrast: more.** A high-contrast theme variant exists
  (sharper borders, less transparency). See the `prefers-contrast: more`
  block in `studio.css`.
- [ ] **Screen-reader landmark sweep.** With VoiceOver (or NVDA / orca)
  open, the page's landmark menu lists banner, primary navigation, main,
  contentinfo. Each lands on the right region.
- [ ] **No color-only meaning.** If your view introduces a chip, pill, or
  badge, the meaning is in the text label too (e.g. status pill says
  "done" or "cancelled", not just colour).

---

## §2. Verification recipes

### §2.1 Lighthouse

```
# In Chrome devtools → Lighthouse → Accessibility, run against the live
# `anneal web` server. Target: 100/100. The 99 we typically see is the
# tap-target audit on monochrome-by-design chips, which is not a real
# failure but flag it in the PR.
anneal web :3001
# open http://localhost:3001/, devtools, Lighthouse, Accessibility.
```

### §2.2 axe-core (optional, no project dep)

Run from the devtools console with axe loaded from the user's own
extension (we do not ship a dep). Zero serious or critical violations is
the gate.

### §2.3 Manual keyboard sweep

1. Reload the page. The first Tab lands on the skip link.
2. Tab through every element. Focus must always be visible.
3. Press `?`. The keyboard-help modal opens. Focus moves to the close
   button.
4. Tab inside the modal. Focus stays inside (focus trap).
5. Press Escape. Modal closes. Focus returns to the trigger.
6. Press `g v` (chord). Visualize view loads. Title and live region
   update.
7. Press `/`. Search input is focused.
8. Press Escape. Search input is blurred.

### §2.4 Manual screen-reader sweep

- **macOS VoiceOver.** Cmd-F5 to enable; use the rotor (VO-U) to list
  landmarks. Expect: banner, navigation (2: primary, breadcrumb), main,
  contentinfo. Navigate to each. Then enable the headings rotor: expect
  exactly one h1 in the rendered view.
- **Linux orca / Windows NVDA.** Same expectations. Press Insert+F7
  (NVDA) for the elements list. Landmarks tab should show the four.

### §2.5 Reduced motion + forced colors

- macOS: System Settings → Accessibility → Display → Reduce motion.
  Reload the page; the wordmark must not animate.
- Windows: Settings → Accessibility → Contrast themes → "Aquatic" or
  similar forced theme. The studio's borders, focus rings, and nav
  active state must still read.

### §2.6 200% zoom

In Chrome devtools, set viewport to 100% then press Cmd-+ until zoom is
200%. Verify:
- No horizontal scroll.
- Nav rail collapses to a top section (covered by the `@media
  (max-width: 720px)` block in `studio.css`).
- All text remains readable.

---

## §3. Verified brand-token contrast (W0 baseline)

Computed against the dark `--bg #14110F` and light `--bg #FBF8F3`
backgrounds, plus the `--surface` panel backgrounds. WCAG 2.2 Level AA
requires 4.5:1 for normal text, 3:1 for large text and UI components.

| token / theme        | hex      | on bg       | on surface  | verdict          |
|----------------------|----------|-------------|-------------|------------------|
| dark text            | #E8E2DA  | 14.61:1     | 13.40:1     | AAA              |
| dark muted           | #8A817A  | 4.93:1      | 4.52:1      | AA (text)        |
| dark faint           | #5C544D  | 2.53:1      | 2.32:1      | UI / dividers only |
| dark teal (forward)  | #00ADD8  | 7.14:1      | 6.54:1      | AAA on bg        |
| dark ember (back)    | #FF7A45  | 7.27:1      | 6.67:1      | AAA on bg        |
| dark gold (fused)    | #F2C57C  | 11.69:1     | 10.71:1     | AAA              |
| light text           | #14110F  | 17.75:1     | 15.55:1     | AAA              |
| light muted          | #5C544D  | 7.00:1      | 6.14:1      | AA               |
| light faint          | #8A817A  | 3.60:1      | 3.16:1      | UI / large-text only |
| light teal (forward) | #006F9E  | 5.26:1      | 4.61:1      | AA               |
| light ember (back)   | #B84A16  | 4.92:1      | 4.31:1      | AA on bg; UI on surface |
| light gold (fused)   | #7A5800  | 6.15:1      | 5.39:1      | AA               |

Notes:

- `--faint` (gray-on-gray) is dividers and decorative hint chips only.
  It is below 4.5:1 by design; it must never carry text meaning.
- `--ember` on the light `--surface` is 4.31:1 (under the 4.5 floor by
  0.19). Ember on surface is used as a chip border in the light theme,
  not as body text. If a future view places ember body text on the
  light surface, switch to ember-on-bg (4.92:1) or darken to `#A3400F`.
- The test `TestWebA11Y_BrandTokenContrast` in
  `cmd/anneal/cmd_web_test.go` recomputes these at build time. If a
  failing ratio appears for a token used as body text, the test STOPS
  the build. Do not edit the token to silence the test; report to
  George and let the brand-token semantics get a deliberate update.

---

## §3a. W2 a11y verification (kernels view)

Phase: W2 kernels view (`/k/<model>`). Verified items below; manual checks
documented as MANUAL where Go-side source-text checks don't cover them.
The W2 test set lives in `cmd/anneal/cmd_web_kernels_test.go`
(10 tests) and `cmd/anneal/cmd_kernels_test.go` (5 tests).

- [x] **Heading hierarchy.** One `<h1 id="view-kernels-heading">` per the
  view; covered by the existing `TestWebA11Y_HeadingHierarchy` sweep
  which counts h1 per `.view` section. No `<h2>` precedes `<h1>` in the
  kernels view.
- [x] **Landmarks present.** No new landmarks introduced; the kernel list
  uses an `<aside aria-label="kernel list">` inside the kernels-view
  section to scope the assistive label, not as a top-level landmark.
- [x] **Skip link.** Inherited from foundation; unchanged.
- [x] **Focus visible.** Inherited; the list items, the diff toggle, and
  the WGSL `<pre>` all rely on the global `:focus-visible` rule.
- [x] **Focus order matches DOM order.** Rail → search → topbar → kernel
  list → kernel detail (header → diff toggle → WGSL pre). Tab through
  in source order. (MANUAL spot-check; no programmatic tabindex >= 0
  introduced.)
- [x] **Keyboard reachable.** The kernel list is a real `<ul
  role="listbox" tabindex="0">`; ArrowDown / ArrowUp / Home / End move
  selection; Enter / Space activate; Escape returns focus to the
  kernels nav rail item. Pinned by `TestWebKernels_KeyboardNav`.
- [x] **No keyboard traps.** Escape on the list returns focus to the nav
  rail. The WGSL `<pre>` is keyboard-scrollable (tabindex=0) and Tab
  exits cleanly to the next interactive element.
- [x] **ARIA labels.** The kernel list has `aria-label="kernels in this
  model"`; the WGSL pre has `aria-label="WGSL source"`; the diff
  toggle has `aria-label` describing the not-yet-wired state. Pinned
  by `TestWebKernels_WGSLPreLabeled` and `TestWebKernels_DiffToggleAriaPressed`.
- [x] **Color contrast.** Token classes use brand vars (`--teal`,
  `--ember`, `--gold`, `--muted`, `--text`); brand-token contrast
  ratios are covered by `TestWebA11Y_BrandTokenContrast`. The
  `--muted` token (used for `.tk-comment` and the gutter "fused"
  carries the documented 4.5+:1 ratio on both bg and surface.
- [x] **DD1: color is never alone.** Tokenizer spans pair colour with
  weight or style: `.tk-keyword { font-weight: 600 }`, `.tk-type {
  font-style: italic }`, `.tk-comment { font-style: italic }`,
  `.tk-attribute { font-weight: 600 }`. The active list item pairs
  the gold left border with a font-weight bump. Pinned by
  `TestWebKernels_TokenizerSpanClasses`.
- [x] **Live region updates.** Kernel selection writes "kernel K3
  selected" to `#live-region` via `announce()`. The "tuned vs
  default" toggle announces "tuned compile pending" today (the
  backend is in W6+). Both go through the same `announce()` channel
  as foundation route changes.
- [x] **Document title.** Inherited from foundation; the route change
  to `/k/<model>` already writes `anneal · kernels`.
- [x] **`aria-current="page"`** on the active nav button. Inherited.
- [x] **`aria-selected`** on the active kernel list item. Set by
  `drawKernelList`. The listbox's `aria-activedescendant` tracks the
  active item by id so screen-reader users hear which option is
  current without focus moving inside the list.
- [x] **Touch target 44x44.** The `.kernel-list-item` and the
  `.diff-toggle` carry `min-height: 44px`; the WGSL pre is a
  scrollable region (no tap target).
- [x] **Text resize.** All sizes are `rem`; the kernels-pane grid
  collapses to a single column under 720px via `@media (max-width:
  720px)`. MANUAL: verified the kernels view at 200% zoom in a real
  browser before landing.
- [x] **Reduced motion.** No animation is introduced in the kernels
  view; the kernel-list item / diff toggle CSS includes a
  `transition: none !important` under `prefers-reduced-motion: reduce`
  as belt-and-braces. The gutter and tokenizer spans never animate.
- [x] **Forced colors.** The tokenizer span colours collapse to
  `CanvasText` under `forced-colors: active`; the weight / italic
  axes carry the distinction (so a screen at high contrast still
  reads "keywords vs comments"). The gutter labels (fwd/bwd/fused)
  fall back to `CanvasText` too; the line-aligned position keeps the
  span structure legible.
- [x] **Prefers contrast: more.** Inherited; the kernels view does not
  introduce its own colour decisions outside the brand tokens, so the
  foundation's `prefers-contrast: more` rule lights up the same way.

DEFERRED / MANUAL:

- Lighthouse + axe-core run on `/k/mlp` is a manual gate before W3 lands;
  the source-text checks above cover the structural contract. The wasm
  artifact must be built first for the real WGSL to render; otherwise the
  pane shows the "wasm not loaded" hint and the live region announces it.
- VoiceOver landmark sweep on `/k/mlp` is a manual gate before W3 lands.
  Expectation: banner, navigation (primary + breadcrumb), main,
  contentinfo. The kernel-list `<aside aria-label="kernel list">` is NOT
  a landmark (aside without `role="complementary"` and inside a section
  reads as a generic group), which is the intended behaviour.

---

## §3b. W3 a11y verification (explain view)

Phase: W3 explain view (`/x/<op>`). Verified items below; manual checks
are flagged MANUAL where Go-side source-text checks don't cover them.
The W3 test set lives in `cmd/anneal/cmd_web_explain_test.go` (11
tests), `cmd/anneal/cmd_explain_test.go` (5 coverage-parity tests), and
`viz/explain_test.go` (11 structural tests on the WASM payload).

The explain view's load-bearing accessibility property: the rules list
and the gradient block are real text content (not images or canvas), so
a screen reader reads the actual rule pattern. The mini-graph SVG carries
shape distinction so meaning survives in forced-colors mode.

- [x] **Heading hierarchy.** One `<h1 id="view-explain-heading">` per the
  view; one `<h2 id="exp-op-name">` for the op name in the detail
  header; three `<h3>` for "rewrite rules", "gradient rule", and
  "before / after". The lede stays a `<p class="lede">`, not a heading,
  so the h1→h2→h3 chain is unbroken. Pinned by
  `TestWebExplain_HeadingHierarchy`.
- [x] **Landmarks present.** No new top-level landmarks; the explain
  view uses `<aside aria-label="op list">` inside the view section to
  scope the assistive label (not as a landmark).
- [x] **Skip link.** Inherited from foundation; unchanged.
- [x] **Focus visible.** Inherited; the search input, the op list, the
  gradient `<pre>`, and the play-rewrite button all rely on the global
  `:focus-visible` rule.
- [x] **Focus order matches DOM order.** Rail → topbar (search +
  device pill + theme + help) → search input (op-search) → op list
  (listbox) → detail section (op name → description → rules list →
  gradient pre → play button). Tab through in source order. (MANUAL
  spot-check; no programmatic tabindex >= 0 introduced.)
- [x] **Keyboard reachable.** The op list is a real `<ul
  role="listbox" tabindex="0">`; ArrowDown / ArrowUp / Home / End move
  selection; Enter / Space activate; Escape returns focus to the
  explain nav rail item. Pinned by `TestWebExplain_KeyboardNavWired`.
- [x] **No keyboard traps.** Escape on the list returns focus to the
  nav rail. The gradient `<pre>` is keyboard-scrollable (tabindex=0)
  and Tab exits cleanly to the play-rewrite button.
- [x] **ARIA labels.** The op search has `aria-label="search ops"` AND
  a redundant `<label for="op-search">` (pinned by
  `TestWebExplain_SearchInputLabeled`); the op list has
  `aria-label="ops in the anneal IR"`; the gradient `<pre>` has
  `aria-label="gradient rule pattern"` (pinned by
  `TestWebExplain_GradientPreLabeled`); the play button has
  `aria-label="play rewrite animation"` (pinned by
  `TestWebExplain_PlayButtonAriaLabel`). The mini-graph SVG carries
  `aria-label="rewrite mini-graph"` and a per-side `<title>` inserted
  on render.
- [x] **Color contrast.** Brand tokens flow through (the view does NOT
  introduce its own colour decisions). The rule-source caption uses
  `--muted` which is documented 4.5+:1 on bg and surface; the
  gradient pre's left border uses `--ember`, which is 7.27:1 on
  dark bg. Pinned by the foundation's
  `TestWebA11Y_BrandTokenContrast`.
- [x] **DD1: color is never alone.** The active op-list item pairs
  the gold left border (`--gold`) with a `font-weight: 600` bump.
  The gradient pre uses an ember left border (3px) AND the section
  heading "gradient rule" so colour is not the sole channel. The
  mini-graph uses three distinct SHAPES (square for Const, circle
  for Var, rounded rect for ALU) so the rewrite is legible even
  with monochrome rendering or for colour-blind users.
- [x] **Live region updates.** Op selection writes "explain: Add
  selected" to `#live-region` via `announce()`. The play-rewrite
  button announces "rule {name} fired" before each animation cycle.
  Both go through the same `announce()` channel as foundation route
  changes. The detail `<article aria-live="polite">` re-announces
  changes inside its own subtree without stealing focus.
- [x] **Document title.** Inherited from foundation; the route change
  to `/x/<op>` already writes `anneal · explain`.
- [x] **`aria-current="page"`** on the active nav button. Inherited.
- [x] **`aria-selected`** on the active op list item. Set by
  `drawOpList`. The listbox's `aria-activedescendant` tracks the
  active item by id so screen-reader users hear which option is
  current without focus moving inside the list.
- [x] **Touch target 44x44.** The `.op-list-item`, the
  `.play-rewrite`, and the search input all carry `min-height: 44px`.
- [x] **Text resize.** All sizes are `rem`; the explain-pane grid
  collapses to a single column under 720px via `@media (max-width:
  720px)`. MANUAL: verified the explain view at 200% zoom in a real
  browser before landing.
- [x] **Reduced motion.** The play-rewrite button branches on
  `window.matchMedia('(prefers-reduced-motion: reduce)').matches` and
  performs an instant swap (no animation, just a 1.2s pause before
  restoring the Before view) when reduced motion is requested. The
  CSS `@media (prefers-reduced-motion: reduce)` block also drops the
  dashed-line "rewriting" hint on the mini-graph SVG. Belt and
  braces.
- [x] **Forced colors.** The mini-graph SVG strokes collapse to
  `CanvasText` and fills to `Canvas` under `forced-colors: active`;
  the rectangular / circular / rounded-rect SHAPE axis carries the
  Const / Var / ALU distinction. The gradient pre's ember left
  border collapses to `CanvasText`. The rules list inherits the
  foundation tokens which already map.
- [x] **Prefers contrast: more.** Inherited; the explain view does not
  introduce its own colour decisions outside the brand tokens, so the
  foundation's `prefers-contrast: more` rule lights up the same way.
- [x] **Mini-graph SVG `aria-label` and per-shape `<title>`.** The
  outer SVG has `aria-label="rewrite mini-graph"` (in HTML) and
  `role="img"`. Each node group gets an inner `<title>` element
  populated on render ("Add: +", "Const: 0", etc.) so a screen-reader
  user inspecting the SVG hears what each shape represents.

DEFERRED / MANUAL:

- Lighthouse + axe-core run on `/x/Add` is a manual gate before W4
  lands; the source-text checks above cover the structural contract.
  Without the wasm artifact, the view shows the "wasm not loaded" hint
  and the live region announces it.
- VoiceOver headings rotor on `/x/Add` should list exactly: h1
  "explain", h2 (op name), h3 "rewrite rules", h3 "gradient rule", h3
  "before / after".
- VoiceOver landmark sweep on `/x/Add`: banner, navigation (primary +
  breadcrumb), main, contentinfo. The op-list `<aside aria-label="op
  list">` is NOT a landmark (intended behaviour).

---

## §3d. W6 a11y verification (train view)

Phase: W6 train view (`/t/<model>`). Verified items below; manual checks
are flagged MANUAL where Go-side source-text checks don't cover them. The
W6 test set lives in `cmd/anneal/cmd_web_train_test.go` (17 tests). The
SSE wire contract is pinned in the same file with a stub trainRunner so
no GPU is required for the gate.

Particular care for SSE-driven live regions: every snapshot updates the
`#train-stats` polite region (NOT assertive, to avoid screen-reader
spam), and the loss SVG's `<desc>` element carries a textual fallback
"loss decreased from X to Y over N samples" rebuilt on each frame.

- [x] **Heading hierarchy.** One `<h1 id="view-train-heading">` per the
  view; the existing `TestWebA11Y_HeadingHierarchy` sweep covers it.
- [x] **Landmarks present.** No new landmarks introduced; train content
  lives inside the inherited `<main>`.
- [x] **Skip link.** Inherited from foundation; unchanged.
- [x] **Focus visible.** Inherited; all controls (select, number input,
  buttons, checkbox, focusable SVG) rely on the global `:focus-visible`
  rule.
- [x] **Focus order matches DOM order.** Rail → search → topbar →
  model select → steps input → start → pause → cancel → bundle
  checkbox → loss SVG (tabindex=0) → open in viz → save run. Tab
  through in source order. (MANUAL spot-check; no programmatic tabindex
  >= 0 introduced other than the figure-content SVG.)
- [x] **Keyboard reachable.** Every control is a real `<select>` /
  `<input>` / `<button>` / `<input type=checkbox>` and reachable by
  Tab. The Enter key activates the start button (HTML default). The
  loss SVG is `tabindex="0"` so a keyboard user can land in it and
  inspect via screen reader.
- [x] **No keyboard traps.** No focus-trap is introduced in the train
  view. Tab moves out of every region without escape required.
- [x] **ARIA labels.** Every form control has a label or aria-label
  pinned by `TestWebTrain_ControlsLabeled`. The loss SVG is
  `role="img"` with `aria-label="loss sparkline"` (pinned by
  `TestWebTrain_SparklineSVGLabeled`); the kernel-thumb SVG is
  `role="img"` with `aria-label="kernel graph thumbnail"`. The
  progressbar carries `aria-label="training progress"`
  (`TestWebTrain_ProgressbarRole`).
- [x] **Progressbar role.** `<div role="progressbar">` with
  `aria-valuemin=0`, `aria-valuemax=100`, and `aria-valuenow` updated
  on each frame. The fill bar reflects the same percentage via
  inline `width`. Pinned by `TestWebTrain_ProgressbarRole`.
- [x] **Live region updates.** `#train-stats` is
  `aria-live="polite"` + `aria-atomic="false"`, so each stat value
  change is announced incrementally without interrupting an
  in-progress utterance. Polite (not assertive) is the deliberate
  choice — assertive would spam screen-reader users on every step.
  Pinned by `TestWebTrain_LiveRegionStats`.
- [x] **Sparkline textual fallback.** The loss SVG has a `<desc
  id="loss-svg-desc">` child that the renderer rewrites every frame
  with "loss decreased from X to Y over N samples". A screen-reader
  user focusing the SVG hears the textual summary instead of having
  to interpret an opaque path. Rebuilt batched in
  `requestAnimationFrame` to avoid flooding the reader.
- [x] **Color contrast.** All token colours use the verified brand
  vars (`--teal`, `--gold`, `--muted`, `--text`); the stat-card
  border colours are decorative-only (the label text is always the
  primary signal — DD1).
- [x] **DD1: color is never alone.** The loss card uses
  `border-left-color: var(--teal)` plus the stat-label "loss". The
  fused card uses `border-left-color: var(--gold)` plus the
  stat-label "fused". The progressbar uses a gradient teal→gold
  paired with the numeric `aria-valuenow` and the percentage in the
  live region. The start button uses gold + a font-weight bump.
- [x] **Document title.** Inherited; the route to `/t/<model>`
  writes `anneal · train`.
- [x] **`aria-current="page"`** on the active nav button. Inherited.
- [x] **Touch target 44x44.** Every control sets `min-height: 44px`
  + a `min-width: 44px` floor in `.train-controls`,
  `.train-actions`, and `.checkbox-label`.
- [x] **Disabled state.** `pause`, `cancel`, `open in viz`, and
  `save run` carry the HTML `disabled` attribute when not actionable;
  the CSS dims them to 0.5 opacity. Each disabled button is also
  unreachable to assistive keyboard navigation by virtue of the HTML
  semantics.
- [x] **Text resize.** All sizes are `rem`; the train-pane uses
  `flex-wrap: wrap` so it reflows at 200% zoom without horizontal
  scroll. MANUAL: verified the train view at 200% zoom in a real
  browser before landing.
- [x] **Reduced motion.** The progress fill's transition is disabled
  under `prefers-reduced-motion: reduce`. The kernel-dot gold pulse
  is replaced by an instant fill-and-stroke change (no animation)
  per the same media block. The sparkline redraws are batched in
  `requestAnimationFrame` (throttling, not motion).
- [x] **Forced colors.** The loss-path stroke falls back to
  `LinkText` under `forced-colors: active`; the stat cards inherit
  the existing `--text`/`Canvas` token mapping from the foundation.
- [x] **Prefers contrast: more.** The train-pane container borders
  upgrade to 2px under `prefers-contrast: more`.

DEFERRED / MANUAL:

- VoiceOver/NVDA sweep of a live train run (with the stub trainRunner
  swapped out for the real WebGPU path) is a manual gate before W7
  lands. Expectation: every snapshot's step + loss are announced
  politely; the sparkline's `<desc>` is read on focus.
- Lighthouse + axe-core run on `/t/mlp` with an active SSE stream is
  a manual gate before W7 lands.

---

## §3c. W4 a11y verification (visualize node inspector)

Phase: W4 visualize view (`/v/<model>`). The studio embeds the existing
viz artifact in an iframe and adds a node-inspector drawer that opens on
click. Verified items below; manual checks are flagged MANUAL where
Go-side source-text checks don't cover them. The W4 test set lives in
`cmd/anneal/cmd_web_visualize_test.go` (12 tests) and
`viz/nodedetail_test.go` (7 tests).

Particular care: the iframe carries a WCAG 4.1.2 `title` attribute; the
drawer captures previous focus on open and restores it on close; the
drawer is `role="region"` with an `aria-label`; the live region announces
"inspecting node X" when the drawer content updates.

- [x] **Heading hierarchy.** One `<h1 id="view-visualize-heading">` per
  the view; the drawer carries an `<h2 id="node-inspector-op">` inside
  it (the drawer is a region, not a view) so the view's h1 stays the
  single h1 in the rendered a11y tree. The existing
  `TestWebA11Y_HeadingHierarchy` sweep covers it.
- [x] **Landmarks present.** No new landmarks introduced; the drawer
  uses `<aside role="region" aria-label="node inspector">` inside the
  visualize view section, scoping its assistive label without
  registering as a top-level landmark.
- [x] **Skip link.** Inherited from foundation; unchanged.
- [x] **Focus visible.** Inherited; the iframe, close button, and any
  drawer-internal focusable element rely on the global `:focus-visible`
  rule. The iframe carries its own focus ring (`#viz-iframe:focus-visible`
  outline-offset:2px) so a keyboard user knows when they have landed
  inside the embedded surface.
- [x] **Focus order matches DOM order.** Rail → search → topbar →
  iframe → drawer-close → drawer h2 (non-focusable) → parent/child
  lists. Tab through in source order. (MANUAL spot-check; no
  programmatic tabindex >= 0 introduced.)
- [x] **Keyboard reachable.** The iframe is `tabindex="0"` so it is
  reachable by Tab. The drawer's close button is the first focusable
  child so Tab lands there immediately on drawer open. Escape closes
  the drawer and returns focus to the previously-active element (or
  the iframe as fallback). Pinned by
  `TestWebVisualize_KeyboardEscapeWiring` and
  `TestWebVisualize_FocusReturnsAfterClose`.
- [x] **No keyboard traps.** The drawer does not trap Tab; tabbing
  past the last drawer element exits to the next view-bar control.
  Escape always exits. The iframe sandbox is
  `allow-scripts allow-same-origin` so the parent's Escape handler is
  not blocked by the embedded page.
- [x] **ARIA labels.** The iframe carries `title="anneal visualize
  timeline"` (WCAG 4.1.2); the drawer carries
  `aria-label="node inspector"`; the close button carries
  `aria-label="close inspector"`. Pinned by
  `TestWebVisualize_IframeSandbox`,
  `TestWebVisualize_DrawerRoleRegion`, and
  `TestWebVisualize_CloseButtonAriaLabel`.
- [x] **Color contrast.** The drawer's dl/ul use the standard `--text`
  / `--muted` brand tokens already covered by
  `TestWebA11Y_BrandTokenContrast`. Parent/child glyphs use the teal
  and ember brand tokens, both above the 4.5:1 ratio on dark bg per
  §3 above.
- [x] **DD1: color is never alone.** Parent vs child distinction is
  carried by a leading `↑` or `↓` glyph (shape) AND a colour (teal /
  ember). The CSS pseudo-element pins both axes; pinned by
  `TestWebVisualize_DrawerCSS`. The iframe's gold `parent-selected`
  highlight pairs colour with a thick stroke-width bump so forced-
  colors users still see the selection.
- [x] **Live region updates.** Opening the drawer writes "inspecting
  node N" to `#live-region` via `announce()`; the drawer's own
  `<dl aria-live="polite">` re-announces when content updates inside an
  already-open drawer (the `aria-live` rather than `aria-atomic="true"`
  keeps the announcement scoped to the new text).
- [x] **Document title.** Inherited; navigating to `/v/<model>` writes
  `anneal · visualize`.
- [x] **`aria-current="page"`** on the active nav button. Inherited.
- [x] **Touch target 44x44.** The close button carries
  `min-height: 44px`; the iframe is a region, not a touch target.
- [x] **Text resize.** All drawer sizes are `rem`; the visualize-pane
  grid collapses to a single column under 720px via
  `@media (max-width: 720px)`. MANUAL: verified the visualize view at
  200% zoom in a real browser before landing.
- [x] **Reduced motion.** The drawer's `ni-slide-in` animation is
  replaced by an instant show under `prefers-reduced-motion: reduce`;
  pinned by `TestWebVisualize_DrawerCSS` (matches both literals). The
  embedded viz artifact already respects reduced motion.
- [x] **Forced colors.** The drawer and iframe borders fall back to
  `CanvasText` under `forced-colors: active`; parent/child glyphs
  collapse to `CanvasText` but the `↑` / `↓` shape carries the
  semantic. The iframe-internal `parent-selected` highlight uses
  `Highlight` under forced colours.
- [x] **Prefers contrast: more.** Drawer + iframe border widths
  double to 2px so they read on high-contrast displays; covered by the
  `prefers-contrast: more` block in `studio.css`.

DEFERRED / MANUAL:

- Lighthouse + axe-core run on `/v/mlp` is a manual gate before W5
  lands; the source-text checks above cover the structural contract.
  The wasm artifact must be built first for the real graph to render;
  otherwise the iframe falls back to the REST `/visualize/api/timeline`
  endpoint (still real-compiler output via DD2).
- VoiceOver landmark sweep on `/v/mlp` is a manual gate before W5
  lands. Expectation: banner, navigation (primary + breadcrumb), main,
  contentinfo. The drawer's `<aside role="region">` registers as a
  region landmark inside the visualize view (the spec wants this), but
  the iframe-internal landmarks are isolated by the sandboxed
  same-origin frame.
- `source_file` / `source_line` are present in the JSON contract but
  always omitted today because the UOp arena does not yet carry
  frontend trace info. The drawer renders an empty `source` row when
  these fields are absent (no error path; no live-region announcement).
  When the trace hook lands, the drawer will surface the click-through
  without further studio-side work.

---

## §4. Cross-references

- `DESIGN.md §11` — the binding a11y invariants (text canonical here).
- `DESIGN.md §1` — brand tokens. Contrast verification lives in this doc.
- `DESIGN.md §3.4` — motion budget. Reduced motion is a hard requirement.
- `DESIGN.md §5` — theme controller. Forced-colors and prefers-contrast
  blocks live in `studio.css` alongside the theme blocks.
- `notes/anneal_web_spec.md §10` — cross-cutting; the "color is never
  alone" rule originates there.
- `cmd/anneal/cmd_web_test.go` — every gate in this doc that can be
  pinned in pure Go has a test. JS state machines and CSS @media
  behaviour are pinned by source-text checks (literals + regex).
