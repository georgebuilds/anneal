version: "2"

linters:
  disable-all: true
  enable:
    - errcheck
    - govet
    - ineffassign
    - misspell
    - staticcheck
    - unconvert
    - goimports
    - unused

linters-settings:
  goimports:
    local-prefixes: github.com/georgebuilds/anneal

issues:
  max-same-issues: 0
